[tool.poetry]
name = "fast-mysql-dump"
version = "0.1.0"
description = ""
authors = ["Your Name <you@example.com>"]
readme = "README.md"
packages = [{ include = "fast_mysql_dump" }]
package-mode = false

[tool.poetry.dependencies]
python = "^3.9"
mysqlclient = "^2.1.1"
paramiko = "^2.12.0"


[build-system]
requires = ["poetry-core"]
build-backend = "poetry.core.masonry.api"
